# 🐧 SMON-LITE

Lightweight CLI system monitor built with Node.js.

## Features
- OS release & version
- Memory usage with ASCII bar
- Real-time CPU usage
- Watch mode (`--watch`)
- JSON output (`--json`)
- No dependencies

## Usage

```bash
node index.js
node index.js --watch
node index.js --json
